using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObstacleBuilder : ScriptableObject
{
    // Use this to build the obstacles based on the character lists passed through. 


    // Take a single obsticle and the parent which it'll be build into.
    public bool BuildObstacle(List<char> obstacleData)
    {
        return true;
    }
}
